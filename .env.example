# Copy to .env and fill in. .env is git-ignored and must never be committed.
#
# Google Safe Browsing v4 Lookup API key.
# Google Cloud Console -> new project -> enable "Safe Browsing API" -> Credentials -> API key.
# Restrict the key and rotate it after the demo (PRD §9).
VITE_SAFE_BROWSING_KEY=
